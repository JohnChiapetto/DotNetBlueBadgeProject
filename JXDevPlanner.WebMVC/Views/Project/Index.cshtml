@model IEnumerable<JXDevPlanner.Models.ProjectListItem>

@{
    ViewBag.Title = "Your Projects";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Desc)
        </th>
        <th></th>
    </tr>
    *@
    <style>
        .project-list-item {
            display: flex;
        }
        .project-list-item .side {
            width: 15%;
        }
        .project-list-item .core {
            width: 85%;
        }
        .project-list-item .core h3 {
            margin-top: 0px;
        }
    </style>
    @foreach (var item in Model)
    {
        <a href="/Project/Details/@item.ProjectID">
            <div class="project-list-item">
                <div class="side">
                    CREATED: @item.CreatedUtc.ToString("MM/dd/yyyy @ hh:mmtt");
                </div>
                <div class="core">
                    <h3>@item.Title</h3>
                    <p>@Html.Raw(item.Desc.Replace("\n","<br>"))</p>
                </div>
            </div>
        </a>
    }
    @*@foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Desc)
            </td>
            <td>
                @Html.ActionLink("Edit","Edit",new { id = item.ProjectID }) |
                @Html.ActionLink("Details","Details",new { id = item.ProjectID }) |
                @Html.ActionLink("Delete","Delete",new { id = item.ProjectID,conf = true })
            </td>
        </tr>
    }

</table>*@
